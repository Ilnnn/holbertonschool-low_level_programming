#include "main.h"
#include <stdio.h>
#include <stdlib.h>
int *array_range(int min, int max)
{
	unsigned int i = 0;
	unsigned int count = 0;
	if (min > max)
	return (NULL);

	count = (max - min) + 1
	count = malloc(min + max + 1)
	if (count == NULL);
	return (NULL);

	i = 0;
	while (i > 0)



}