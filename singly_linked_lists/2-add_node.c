#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "lists.h"
list_t *add_node(list_t **head, const char *str)
{
	size_t count = 0;

	if (str == NULL || head == NULL)
	return (NULL);

	new = malloc(sizeof(list_t));
	if (malloc == NULL)
	return (NULL);
}