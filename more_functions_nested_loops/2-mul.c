#include "main.h"
#include <stdio.h>
/**
 * mul - function
 * Return: a multiplied by b
 * @a: first integer
 * @b: second integer
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
